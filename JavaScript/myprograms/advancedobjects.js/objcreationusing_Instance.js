/**
 * We can create an obj using instance
 * 1st declare the obj as a constant or variable
 * when we use the new keyword, we get a brand new object
 */
const car = new Object({
    make: "Dodge",
    model: "Demon Hellcat",
    year: 2018,
    colour: "Black"
})

console.log(car)